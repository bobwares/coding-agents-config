# ADR — Turn 26

No architectural decision made this turn — created a standalone `repo-analysis` skill as an alternative to the existing `cheap-analysis` skill's codebase template. The new skill is single-purpose and self-contained with an inline template.
