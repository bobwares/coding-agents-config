create a document docs/agents.md that contains a table of each agent in the agents directory.  col1 name col2 purpose col3 triggers col4 hooks


create a document docs/hooks.md that contains a table of each hook defined under .claude directory.  col1 name col2 purpose col3 triggers col4 what skills, agents  it use.


create a document docs/skills.md that contains a table of each skill.md defined under .claude directory.  col1 name col2 purpose col3 triggers col4 what skills, agents  use it.
